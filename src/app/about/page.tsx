import styles from './page.module.scss';
import cx from 'classnames';

export default function AboutPage() {
  // About Us Header
  // We The People
  // ImageText - transparent
  // FOOTER

  return <main className={cx(styles['container'], 'page')}></main>;
}
